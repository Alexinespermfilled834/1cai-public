# 📂 Public vs Private Repository Guide

**Дата:** 6 ноября 2025

---

## 🎯 Цель документа

Четкое разделение между тем, что публикуется в open-source репозитории (`1cai-public`) и что остается конфиденциальным (`1cai-business-internal`).

---

## ✅ Что ПУБЛИКУЕТСЯ (public repo)

### 1. Open-Source Code
**Весь функциональный код**

✅ **Можно публиковать:**
- `src/` - весь исходный код (Python, TypeScript)
- `tests/` - юнит и интеграционные тесты
- `scripts/` - утилиты и скрипты (кроме `scripts/tiers/`)
- `execution-env/` - код для code execution
- `edt-plugin/` - плагин для Eclipse EDT

### 2. Техническая документация
**Как использовать и развернуть систему**

✅ **Можно публиковать:**
- `docs/01-getting-started/` - инструкции по установке
- `docs/02-architecture/` - архитектура системы (без бизнес-деталей)
- `docs/03-ai-agents/` - описание AI агентов
- `docs/04-deployment/` - как развернуть
- `docs/05-development/` - для разработчиков
- `docs/06-features/` - описание функций
- `docs/08-code-execution/` - Code Execution Guide
- `docs/API_REFERENCE.md` - API документация
- `CONTRIBUTING.md` - как внести вклад
- `SECURITY.md` - security best practices

### 3. Инфраструктура (базовая)
**Docker и Kubernetes конфигурации**

✅ **Можно публиковать:**
- `docker-compose.yml` - базовая конфигурация
- `docker-compose.mvp.yml` - минимальная версия
- `docker-compose.dev.yml` - для разработки
- `k8s/deployments/` - Kubernetes манифесты (общие)
- `Dockerfile.*` - Docker образы

### 4. Примеры и шаблоны
**Для пользователей**

✅ **Можно публиковать:**
- `examples/` - примеры использования
- `templates/` - шаблоны кода
- `env.example` - пример .env файла

### 5. Общие описания возможностей
**Без конкретных цен и ROI**

✅ **Можно публиковать:**
- "Система поддерживает масштабирование"
- "Доступны различные deployment options"
- "Гибкая архитектура с вертикальным и горизонтальным масштабированием"
- "Поддержка от малого бизнеса до корпораций"

---

## ❌ Что НЕ публикуется (private repo)

### 1. Бизнес-планы и стратегии
**Конфиденциальная бизнес-информация**

❌ **НЕ публиковать:**
- `private/business/scaling/` - **стратегия масштабирования с ценами**
  - `SCALING_STRATEGY.md` - детальные цены, ROI, бизнес-планы
  - `README.md` - навигация с ценами
  - `SCALING_IMPLEMENTATION_COMPLETE.md` - отчет о реализации

### 2. Tier конфигурации
**Детальные лимиты и pricing**

❌ **НЕ публиковать:**
- `private/config/tiers/` - **конфигурации tier'ов**
  - `free.yaml` - лимиты, pricing FREE tier
  - `starter.yaml` - лимиты, pricing STARTER tier
  - `professional.yaml` - лимиты, pricing PROFESSIONAL tier
  - `business.yaml` - лимиты, pricing BUSINESS tier
  - `enterprise.yaml` - лимиты, pricing ENTERPRISE tier

### 3. Deployment конфигурации для tier'ов
**Специфичные для платных планов**

❌ **НЕ публиковать:**
- `docker-compose.tier-*.yml` - Docker Compose для конкретных tier'ов
- `k8s/tiers/` - Kubernetes манифесты для tier'ов
- `scripts/tiers/` - скрипты миграции между tier'ами

### 4. ROI расчеты и ценообразование
**Коммерческая информация**

❌ **НЕ публиковать:**
- Конкретные цены tier'ов ($49, $249, $1,499, etc.)
- ROI расчеты (408%, 2008%, 2667%)
- Формулы расчета экономии для клиентов
- Margin analysis
- Revenue прогнозы
- Cost structure (сколько стоит обслуживание tier'а)

### 5. Детальные лимиты tier'ов
**Могут быть использованы конкурентами**

❌ **НЕ публиковать:**
- Точные лимиты запросов/день для каждого tier'а
- Storage quotas
- Rate limits (requests/minute)
- CPU/RAM allocations для каждого tier'а
- Database connection limits

### 6. Go-to-market планы
**Стратегическая информация**

❌ **НЕ публиковать:**
- Планы захвата рынка
- Segmentation strategy
- Партнерские программы
- Marketing campaigns
- Sales strategies

### 7. Roadmap с бюджетами
**Внутренние планы**

❌ **НЕ публиковать:**
- Roadmap с финансовыми прогнозами
- Бюджеты на реализацию фич
- Приоритизация по revenue
- Investment plans

---

## 📊 Сравнительная таблица

| Категория | Public Repo | Private Repo |
|-----------|-------------|--------------|
| **Исходный код** | ✅ Весь код | ❌ - |
| **API документация** | ✅ Полная | ❌ - |
| **Архитектура** | ✅ Техническая | ❌ Бизнес-стратегия |
| **Deployment guides** | ✅ Общие инструкции | ❌ Tier-specific configs |
| **Примеры** | ✅ Все примеры | ❌ - |
| **Pricing** | ❌ - | ✅ Детальные цены |
| **ROI расчеты** | ❌ - | ✅ Формулы и прогнозы |
| **Tier конфигурации** | ❌ - | ✅ YAML с лимитами |
| **Go-to-market** | ❌ - | ✅ Стратегия захвата рынка |
| **Roadmap** | ✅ Общий (без бюджетов) | ✅ Детальный (с финансами) |

---

## 🔐 .gitignore правила

### В `.gitignore` добавлено:

```gitignore
# === КРИТИЧНО: Бизнес-планы и pricing (ТОЛЬКО для приватного репозитория!) ===

# Приватная директория (ВЕСЬ контент - конфиденциальный!)
private/
/private/

# Tier configurations с ценами
config/tiers/
/config/tiers/

# Документация по масштабированию с ценами
docs/10-scaling/
/docs/10-scaling/

# Docker compose файлы для tier'ов
docker-compose.tier-*.yml

# Kubernetes манифесты для tier'ов
k8s/tiers/

# Скрипты миграции между tier'ами
scripts/tiers/

# Внутренние отчеты о реализации
*SCALING_IMPLEMENTATION*.md
*TIER_IMPLEMENTATION*.md

# Бизнес-планы и стратегии
*BUSINESS_PLAN*.md
*PRICING_STRATEGY*.md
*MONETIZATION*.md
*REVENUE*.md
```

---

## 🔄 Workflow для двух репозиториев

### Setup:

```bash
# 1. Public repo (уже есть)
git remote -v
# origin  https://github.com/DmitrL-dev/1cai-public.git

# 2. Добавить private repo
git remote add private git@github.com:YourOrg/1cai-business-internal.git

# 3. Проверить
git remote -v
# origin    https://github.com/DmitrL-dev/1cai-public.git
# private   git@github.com:YourOrg/1cai-business-internal.git
```

### Daily workflow:

```bash
# Работа с кодом (public)
git add src/ docs/ tests/
git commit -m "Add new feature"
git push origin main

# Работа с бизнес-планами (private)
git add private/
git commit -m "Update pricing strategy"
git push private main
```

### Проверка перед push:

```bash
# ⚠️ ВСЕГДА проверять перед push в public!
git status | grep -E "(private|tier|pricing|scaling)"

# Если выводит что-то - НЕ pushить! Это конфиденциально
# Если ничего не выводит - OK для public push
```

---

## 📋 Checklist перед публикацией

### Перед КАЖДЫМ `git push origin main`:

- [ ] Запустить проверку: `git status | grep -E "(private|tier|pricing)"`
- [ ] Убедиться что `private/` в .gitignore
- [ ] Проверить что нет упоминаний конкретных цен в commits
- [ ] Проверить что нет ROI расчетов в документации
- [ ] Проверить что нет tier-конфигураций (.yaml файлов с ценами)
- [ ] Review diff: `git diff origin/main`

### Дополнительная проверка:

```bash
# Поиск случайных упоминаний цен в staging
git diff --cached | grep -E "\$[0-9]{2,}" && echo "⚠️ НАЙДЕНЫ ЦЕНЫ!" || echo "✅ OK"

# Поиск ROI
git diff --cached | grep -i "roi" && echo "⚠️ НАЙДЕН ROI!" || echo "✅ OK"

# Поиск tier configurations
git diff --cached | grep -E "tier_(name|price)" && echo "⚠️ НАЙДЕНЫ TIER CONFIGS!" || echo "✅ OK"
```

---

## ⚠️ Что делать если случайно запушили конфиденциальное?

### Если push еще не сделан:

```bash
# Unstage файлы
git reset HEAD private/
git reset HEAD config/tiers/

# Проверить
git status
```

### Если push уже сделан в public repo:

```bash
# ⚠️ СРОЧНО!

# 1. Удалить файлы из git (но оставить локально)
git rm --cached -r private/
git rm --cached -r config/tiers/
git commit -m "Remove confidential files"
git push origin main

# 2. Очистить историю (если файлы были в предыдущих commits)
git filter-branch --tree-filter 'rm -rf private config/tiers' HEAD
git push origin main --force

# ⚠️ ВНИМАНИЕ: --force опасен! Координировать с командой!

# 3. Ротировать все secrets (на всякий случай)
# - Изменить API ключи
# - Изменить пароли БД
# - Изменить Telegram bot tokens
```

---

## 💡 Best Practices

### 1. Разные .env файлы

**Public repo:**
```bash
# env.example (шаблон, БЕЗ реальных значений)
TELEGRAM_BOT_TOKEN=your_token_here
OPENAI_API_KEY=your_key_here
POSTGRES_PASSWORD=changeme
```

**Private repo:**
```bash
# .env.production (РЕАЛЬНЫЕ значения)
TELEGRAM_BOT_TOKEN=123456:ABC-DEF...
OPENAI_API_KEY=sk-...
POSTGRES_PASSWORD=Secure_P@ssw0rd!123
```

### 2. Разные README

**Public repo README.md:**
```markdown
## 🚀 Quick Start

1. Clone repo
2. Copy `env.example` to `.env`
3. Fill in your credentials
4. Run `docker-compose up -d`
```

**Private repo README.md:**
```markdown
## 🚀 Deployment для клиентов

### STARTER tier ($49/mo)
- Deploy: `docker-compose -f tier-starter.yml up -d`
- Limits: 1K req/day, 3 configurations
- ROI: 408%

### PROFESSIONAL tier ($249/mo)
- Deploy: `kubectl apply -f k8s/tiers/professional/`
- Limits: 10K req/day, 10 configurations
- ROI: 2008%
```

### 3. Отдельные branches

```bash
# Public repo
main - production code
develop - development
feature/* - новые фичи

# Private repo
main - бизнес-планы
pricing/* - изменения в pricing
strategy/* - новые стратегии
```

---

## 📞 Контакты для вопросов

**По поводу что публиковать:**
- 💬 Internal Slack: #open-source-governance
- 📧 Email: opensource@internal.local

**По поводу бизнес-планов:**
- 💬 Internal Slack: #business-planning
- 📧 Email: business@internal.local

---

**Создано:** 6 ноября 2025  
**Автор:** AI Assistant  
**Статус:** Active Policy  
**Next Review:** Декабрь 2025

