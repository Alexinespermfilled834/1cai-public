# 📂 Структура репозиториев проекта 1C AI Stack

**Дата:** 6 ноября 2025

---

## 🎯 Два репозитория

Проект использует **два отдельных репозитория**:

### 1. 🌐 Публичный (Open-Source)
**URL:** https://github.com/DmitrL-dev/1cai-public

**Содержит:**
- ✅ Весь исходный код (Python, TypeScript, Java)
- ✅ Техническую документацию
- ✅ Инструкции по установке и развертыванию
- ✅ Примеры использования
- ✅ API документацию
- ✅ Contributing guidelines

**НЕ содержит:**
- ❌ Конкретные цены tier'ов
- ❌ ROI расчеты
- ❌ Бизнес-стратегии
- ❌ Детальные tier конфигурации

**Аудитория:**
- Open-source community
- Потенциальные пользователи
- Разработчики (contributors)

---

### 2. 🔒 Приватный (Business)
**URL:** https://github.com/DmitrL-dev/1cai

**Содержит:**
- ✅ Весь код из публичного репозитория
- ✅ + Бизнес-планы и стратегии
- ✅ + Pricing tier'ов (FREE, STARTER, PROFESSIONAL, BUSINESS, ENTERPRISE)
- ✅ + ROI расчеты (408% - 2667%+ возврат инвестиций)
- ✅ + Tier конфигурации (YAML с детальными лимитами)
- ✅ + Docker/Kubernetes конфигурации для tier'ов
- ✅ + Скрипты миграции между tier'ами
- ✅ + Внутренние инструменты и скрипты

**Структура (дополнительно к public):**
```
private/
├── business/
│   └── scaling/
│       ├── SCALING_STRATEGY.md          # Полная стратегия с ценами
│       └── README.md                     # Навигация
├── config/
│   └── tiers/
│       ├── free.yaml                     # FREE tier конфигурация
│       ├── starter.yaml                  # STARTER tier ($49/mo)
│       ├── professional.yaml             # PROFESSIONAL tier ($249/mo)
│       ├── business.yaml                 # BUSINESS tier ($1,499/mo)
│       └── enterprise.yaml               # ENTERPRISE tier ($7,500+/mo)
└── README.md                             # Объяснение приватной директории

config/tiers/                             # Tier конфигурации (если не в private/)
docker-compose.tier-*.yml                 # Docker Compose для tier'ов
k8s/tiers/                                # Kubernetes манифесты для tier'ов
scripts/tiers/                            # Скрипты миграции и валидации
```

**Аудитория:**
- Внутренняя команда
- Партнеры (под NDA)
- Инвесторы

---

## 🔄 Workflow

### Работа с публичным кодом

```bash
# 1. Клонировать публичный репозиторий
git clone https://github.com/DmitrL-dev/1cai-public.git
cd 1cai-public

# 2. Разработка новой фичи
git checkout -b feature/new-feature
# ... код ...
git commit -m "Add new feature"

# 3. Push в публичный repo
git push origin feature/new-feature

# 4. Создать Pull Request на GitHub
```

### Работа с бизнес-планами

```bash
# 1. Клонировать приватный репозиторий
git clone https://github.com/DmitrL-dev/1cai.git
cd 1cai

# 2. Создать/обновить бизнес-планы
mkdir -p private/business/scaling
nano private/business/scaling/SCALING_STRATEGY.md

# 3. Commit и push в приватный repo
git add private/
git commit -m "Update pricing strategy"
git push origin main
```

### Синхронизация между репозиториями

**Из public в private (технический код):**

```bash
# В приватном репозитории (1cai)
git remote add public https://github.com/DmitrL-dev/1cai-public.git
git fetch public
git merge public/main

# Или cherry-pick конкретных коммитов
git cherry-pick <commit-hash>
```

**Из private в public (только технический код, БЕЗ бизнес-планов):**

```bash
# В публичном репозитории (1cai-public)
git remote add private https://github.com/DmitrL-dev/1cai.git
git fetch private

# Cherry-pick ТОЛЬКО технических коммитов (без business/)
git cherry-pick <commit-hash-technical-only>

# ⚠️ НИКОГДА не делать:
# git merge private/main  # Это добавит бизнес-планы в public!
```

---

## 🛡️ Защита от случайной публикации

### В публичном репозитории (1cai-public)

**`.gitignore` содержит:**
```gitignore
# Бизнес-планы и pricing
private/
config/tiers/
docs/10-scaling/  # с ценами
docker-compose.tier-*.yml
k8s/tiers/
scripts/tiers/
*SCALING_IMPLEMENTATION*.md
*BUSINESS_PLAN*.md
*PRICING_STRATEGY*.md
```

**Результат:** Даже если случайно создать файлы с ценами локально, они не попадут в public repo.

### В приватном репозитории (1cai)

**`.gitignore` НЕ блокирует** бизнес-файлы:
```gitignore
# Обычные правила (credentials, logs, etc.)
.env
*.log
__pycache__/

# НО НЕ блокирует:
# private/ - доступно
# config/tiers/ - доступно
```

---

## 📋 Checklist перед публикацией

### Перед `git push origin main` в 1cai-public:

- [ ] Проверить: `git status | grep -E "(private|tier|pricing)"`
- [ ] Проверить diff: `git diff origin/main`
- [ ] Убедиться что нет упоминаний конкретных цен
- [ ] Убедиться что нет ROI расчетов
- [ ] Убедиться что нет tier-конфигураций
- [ ] Review коммита на GitHub перед merge

### Автоматическая проверка:

```bash
# Поиск случайных цен
git diff --cached | grep -E "\$[0-9]{2,}" && echo "⚠️ НАЙДЕНЫ ЦЕНЫ!" || echo "✅ OK"

# Поиск ROI
git diff --cached | grep -i "roi.*[0-9]+%" && echo "⚠️ НАЙДЕН ROI!" || echo "✅ OK"

# Поиск tier configs
git diff --cached | grep -E "tier_(price|name)" && echo "⚠️ TIER CONFIG!" || echo "✅ OK"
```

---

## 📊 Сравнение репозиториев

| Аспект | 1cai-public (публичный) | 1cai (приватный) |
|--------|-------------------------|------------------|
| **Видимость** | 🌐 Public (весь интернет) | 🔒 Private (только team) |
| **Исходный код** | ✅ Весь код | ✅ Весь код |
| **Документация** | ✅ Техническая | ✅ Техническая + Бизнес |
| **Pricing** | ❌ Нет цен | ✅ Детальные цены |
| **ROI расчеты** | ❌ Нет | ✅ Полные расчеты |
| **Tier конфигурации** | ❌ Нет | ✅ 5 YAML файлов |
| **Бизнес-стратегии** | ❌ Нет | ✅ Полные планы |
| **Contributors** | ✅ Community | ❌ Только team |
| **Issues** | ✅ Публичные | ❌ Приватные |
| **Stars/Forks** | ✅ Да | ❌ Нет (приватный) |

---

## 🚀 Quick Start

### Для разработчика (публичный код):

```bash
# 1. Fork публичного репозитория на GitHub
# 2. Clone
git clone https://github.com/YOUR_USERNAME/1cai-public.git
cd 1cai-public

# 3. Setup
cp env.example .env
pip install -r requirements.txt

# 4. Develop
git checkout -b feature/my-feature
# ... код ...

# 5. Submit PR
git push origin feature/my-feature
# Создать PR на GitHub
```

### Для команды (с доступом к приватному):

```bash
# 1. Clone приватного репозитория
git clone https://github.com/DmitrL-dev/1cai.git
cd 1cai

# 2. Setup (включая бизнес-файлы)
cp env.example .env
# ... credentials ...

# 3. Доступ ко всему
ls private/business/scaling/  # Бизнес-планы
ls config/tiers/              # Tier конфигурации
```

---

## 🔑 Доступ к репозиториям

### Публичный (1cai-public)
- **Чтение:** Все (публичный доступ)
- **Запись:** Только maintainers (после review PR)

### Приватный (1cai)
- **Чтение:** Только team members
- **Запись:** Только team members
- **Управление:** Repository admins

**Как добавить в team:**
1. GitHub → Settings → Collaborators
2. Invite user
3. Set permissions (Read/Write/Admin)

---

## 📞 Вопросы

**По техническому коду:**
- 💬 [GitHub Discussions](https://github.com/DmitrL-dev/1cai-public/discussions) (public)
- 🐛 [Issues](https://github.com/DmitrL-dev/1cai-public/issues) (public)

**По бизнес-планам:**
- 📧 Email: business@internal.local
- 💬 Internal Slack: #business-planning

---

**Создано:** 6 ноября 2025  
**Автор:** AI Assistant  
**Next Review:** Декабрь 2025

